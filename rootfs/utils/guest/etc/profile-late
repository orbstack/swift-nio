__path_set=0
case "$PATH" in
*"/opt/macvirt-guest/bin"*)
__path_set=1
;;
esac

if [ $__path_set = 0 ]; then
    export PATH="/opt/macvirt-guest/bin-hiprio:$PATH:/opt/macvirt-guest/bin"
fi

unset __path_set
